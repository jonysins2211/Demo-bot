# FileStream bot clients registry
# The main Bot instance is imported from bot.py; multi_clients and work_loads
# are populated by initialize_clients() during startup.

multi_clients = {}
work_loads = {}
